---
- name: "Oracle - Resumen BDs con Restore Points Activos"
  hosts: "{{ target_host | default('oracle_single_instance') }}"
  gather_facts: true
  serial: 5
  vars:
    oracle_user:   "/ as sysdba"
    report_dir:    "/tmp/oracle_rp_check"
    log_file:      "restore_points_{{ ansible_date_time.date }}.log"
    local_out_dir: "/var/ansible/oracle_reports/restore_points"
    resumen_file:  "{{ local_out_dir }}/RESUMEN_CON_RP_{{ ansible_date_time.date }}.txt"

  pre_tasks:

    - name: "PRE | Crear directorio temporal en servidor"
      file:
        path: "{{ report_dir }}"
        state: directory
        owner: "{{ oracle_os_user | default('oracle') }}"
        mode: '0750'

    - name: "PRE | Crear directorio local en controller"
      file:
        path: "{{ local_out_dir }}"
        state: directory
        mode: '0755'
      delegate_to: localhost
      run_once: true

    - name: "PRE | Inicializar fichero de resumen (solo primera vez)"
      ansible.builtin.copy:
        dest: "{{ resumen_file }}"
        content: |
          ================================================================================
           BASES DE DATOS CON RESTORE POINTS ACTIVOS
           Fecha: {{ ansible_date_time.date }}
          ================================================================================
        force: false
        mode: '0644'
      delegate_to: localhost
      run_once: true

  tasks:

    - name: "1 | Consultar restore points"
      shell: |
        export ORACLE_HOME={{ oracle_home }}
        export ORACLE_SID={{ oracle_sid }}
        export PATH=$ORACLE_HOME/bin:$PATH
        sqlplus -s "{{ oracle_user }}" << 'EOF'
        SET PAGESIZE 0 FEEDBACK OFF VERIFY OFF HEADING OFF ECHO OFF LINESIZE 300
        ALTER SESSION SET NLS_DATE_FORMAT='DD/MM/YYYY HH24:MI:SS';
        SELECT
            NVL(p.name, 'CDB$ROOT')                         || '|' ||
            rp.name                                         || '|' ||
            rp.guarantee_flashback_database                 || '|' ||
            TO_CHAR(rp.time,'DD/MM/YYYY HH24:MI:SS')       || '|' ||
            ROUND((SYSDATE - rp.time), 2)                  || '|' ||
            NVL(rp.storage_size, 0)                        || '|' ||
            rp.scn
        FROM cdb_restore_points rp
        LEFT JOIN v$pdbs p ON rp.con_id = p.con_id
        ORDER BY p.name NULLS FIRST, rp.time DESC;
        EXIT;
        EOF
      register: r_restore_points
      changed_when: false

    - name: "1 | Consultar info general de la BD"
      shell: |
        export ORACLE_HOME={{ oracle_home }}
        export ORACLE_SID={{ oracle_sid }}
        export PATH=$ORACLE_HOME/bin:$PATH
        sqlplus -s "{{ oracle_user }}" << 'EOF'
        SET PAGESIZE 0 FEEDBACK OFF VERIFY OFF HEADING OFF ECHO OFF LINESIZE 200
        SELECT
            d.name           || '|' ||
            d.db_unique_name || '|' ||
            i.host_name      || '|' ||
            i.version_full
        FROM v$database d, v$instance i;
        EXIT;
        EOF
      register: r_db_info
      changed_when: false

    - name: "2 | Parsear restore points"
      set_fact:
        rp_lines: >-
          {{ r_restore_points.stdout_lines
             | select('match', '.+\|.+')
             | map('trim') | list }}

    - name: "2 | Parsear info de BD"
      set_fact:
        db_info_parts: "{{ r_db_info.stdout | trim | split('|') }}"

    # Solo escribe en el resumen si hay restore points
    - name: "3 | Añadir al resumen solo BDs con restore points"
      blockinfile:
        path: "{{ resumen_file }}"
        marker: "# {mark} {{ inventory_hostname }}_{{ oracle_sid }}"
        block: |

          Host         : {{ inventory_hostname }}
          BD           : {{ db_info_parts[0] }} ({{ db_info_parts[1] }})
          Versión      : {{ db_info_parts[3] }}
          ──────────────────────────────────────────────────────────────────────────────
          {% for line in rp_lines %}
          {% set c = line.split('|') %}
            · Contenedor : {{ c[0] }}
              Nombre     : {{ c[1] }}
              Creado     : {{ c[3] }}
              Antigüedad : {{ c[4] | float | round(1) }} días
              SCN        : {{ c[6] }}
              Guaranteed : {{ c[2] }}
              Storage    : {{ (c[5] | int / 1073741824) | round(2) }} GB
          {% endfor %}
        insertafter: EOF
        mode: '0644'
      delegate_to: localhost
      when: rp_lines | length > 0

  post_tasks:

    - name: "POST | Añadir pie de resumen al final"
      lineinfile:
        path: "{{ resumen_file }}"
        line: |

          ================================================================================
           FIN DEL RESUMEN - {{ ansible_date_time.iso8601 }}
          ================================================================================
        insertafter: EOF
      delegate_to: localhost
      run_once: true
=================================================================================================================================
---------------   OTRA
==================================================================================================================================
- name: "1 | Consultar restore points por cada CDB"
      ansible.builtin.shell: |
        export ORACLE_HOME={{ item.oracle_home }}
        export ORACLE_SID={{ item.oracle_sid }}
        export PATH=$ORACLE_HOME/bin:$PATH
        sqlplus -s "{{ oracle_user }}" << 'EOF'
        SET PAGESIZE 0 FEEDBACK OFF VERIFY OFF HEADING OFF ECHO OFF LINESIZE 300
        ALTER SESSION SET NLS_DATE_FORMAT='DD/MM/YYYY HH24:MI:SS';
        SELECT
            CASE
              WHEN rp.con_id = 1 THEN 'CDB$ROOT'
              WHEN rp.con_id = 2 THEN 'PDB$SEED'
              ELSE NVL(p.name, 'UNKNOWN')
            END                                       || '|' ||
            CASE
              WHEN rp.con_id = 1 THEN 'CDB'
              ELSE 'PDB'
            END                                       || '|' ||
            rp.name                                   || '|' ||
            rp.guarantee_flashback_database           || '|' ||
            TO_CHAR(rp.time,'DD/MM/YYYY HH24:MI:SS') || '|' ||
            ROUND((SYSDATE - rp.time), 2)             || '|' ||
            NVL(rp.storage_size, 0)                  || '|' ||
            rp.scn                                   || '|' ||
            NVL(rp.pdb_restore_point, 'NO')          || '|' ||
            NVL(rp.clean_pdb_restore_point, 'NO')
        FROM cdb_restore_points rp
        LEFT JOIN v$pdbs p ON rp.con_id = p.con_id
        ORDER BY rp.con_id, rp.time DESC;
        EXIT;
        EOF
      register: r_restore_points
      changed_when: false
      become: true
      become_user: "{{ oracle_os_user | default('oracle') }}"
      loop: "{{ oracle_databases }}"
      loop_control:
        label: "{{ item.oracle_sid }}"
- name: "2 | Escribir en resumen solo CDBs/PDBs con restore points"
      ansible.builtin.blockinfile:
        path: "{{ resumen_file }}"
        marker: "# {mark} {{ inventory_hostname }}_{{ item.item.oracle_sid }}"
        block: |

          Host         : {{ inventory_hostname }}
          CDB          : {{ item.item.oracle_sid }}
          BD Info      : {{ (r_db_info.results[idx].stdout | trim).split('|')[0] }} ({{ (r_db_info.results[idx].stdout | trim).split('|')[1] }})
          Versión      : {{ (r_db_info.results[idx].stdout | trim).split('|')[3] }}
          ──────────────────────────────────────────────────────────────────────────────
          {% set rp_list = item.stdout_lines | select('match', '.+\|.+') | list %}
          {% set cdb_rps = rp_list | select('match', '^[^|]+\|CDB\|.*') | list %}
          {% set pdb_rps = rp_list | select('match', '^[^|]+\|PDB\|.*') | list %}

          {% if cdb_rps | length > 0 %}
          [CDB - Restore Points]
          {% for line in cdb_rps %}
          {% set c = line.split('|') %}
            · Contenedor : {{ c[0] }}
              Nombre     : {{ c[2] }}
              Creado     : {{ c[4] }}
              Antigüedad : {{ c[5] | float | round(1) }} días
              SCN        : {{ c[7] }}
              Guaranteed : {{ c[3] }}
              Storage    : {{ (c[6] | int / 1073741824) | round(2) }} GB
          {% endfor %}
          {% endif %}

          {% if pdb_rps | length > 0 %}
          [PDB - Restore Points]
          {% for line in pdb_rps %}
          {% set c = line.split('|') %}
            · PDB        : {{ c[0] }}
              Nombre     : {{ c[2] }}
              Creado     : {{ c[4] }}
              Antigüedad : {{ c[5] | float | round(1) }} días
              SCN        : {{ c[7] }}
              Guaranteed : {{ c[3] }}
              Storage    : {{ (c[6] | int / 1073741824) | round(2) }} GB
              Es PDB RP  : {{ c[8] }}
              Clean PDB  : {{ c[9] }}
          {% endfor %}
          {% endif %}
        insertafter: EOF
        mode: '0644'
      delegate_to: localhost
      loop: "{{ r_restore_points.results }}"
      loop_control:
        index_var: idx
        label: "{{ item.item.oracle_sid }}"
      when: item.stdout_lines | select('match', '.+\|.+') | list | length > 0
```

El resumen resultante quedará así:
```
================================================================================
 BASES DE DATOS CON RESTORE POINTS ACTIVOS
 Fecha: 2026-02-22
================================================================================

Host         : oradb01
CDB          : CDB1
BD Info      : CDB1 (CDB1_UNIQUE)
Versión      : 19.0.0.0.0
──────────────────────────────────────────────────────────────────────────────
[CDB - Restore Points]
  · Contenedor : CDB$ROOT
    Nombre     : RP_BEFORE_PATCH
    Creado     : 15/02/2026 10:30:00
    Antigüedad : 7.2 días
    SCN        : 12345678
    Guaranteed : YES
    Storage    : 0.5 GB

[PDB - Restore Points]
  · PDB        : PDB_VENTAS
    Nombre     : RP_PDB_VENTAS_PRE
    Creado     : 18/02/2026 08:00:00
    Antigüedad : 4.1 días
    SCN        : 12349999
    Guaranteed : NO
    Storage    : 0.12 GB
    Es PDB RP  : YES
    Clean PDB  : NO
